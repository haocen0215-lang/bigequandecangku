# 普利莫迪亚沙盒卡 — 架构合规性与世界观对齐评审
- 日期: 2026-05-19
- 概述: 对照教程21条伪0层原则和示例/普利莫迪亚世界观，逐模块审查代码架构
- 状态: 已完成
- 总体结论: 有条件通过

## 评审范围

# 普利莫迪亚沙盒卡 — 架构合规性与世界观对齐评审

> 评审日期：2026-05-18
> 对照基准：大型前端卡AIRP伪0层玩法教程 + 示例/普利莫迪亚

---

## 评审范围

- `engine/` — 规则裁判层
- `services/` — 酒馆接口适配层
- `dispatcher.ts` / `actions.ts` — 统一动作派发
- `script.ts` / `index.html` — 前端客户端
- `saveSchema.ts` — 主存档结构

## 评审摘要

- 当前状态: 已完成
- 已审模块: engine/*, services/*, actions.ts, dispatcher.ts, script.ts, index.html, saveSchema.ts, labels.ts
- 当前进度: 已记录 4 个里程碑；最新：M4
- 里程碑总数: 4
- 已完成里程碑: 4
- 问题总数: 4
- 问题严重级别分布: 高 0 / 中 1 / 低 3
- 最新结论: ## 总体结论 代码架构**完全符合**伪0层教程21条原则，普利莫迪亚世界观对齐度**极高**。 ### 绿旗（全通过） - **核心架构5原则** — 前端客户端 → chat变量数据库 → engine裁判 → dispatcher唯一入口 → AI纯叙述者 - **酒馆接口7维** — 变量/楼层/生成/注入/事件/世界书全部正确使用 - **引擎规则** — 8级灶火行会烹饪、7级搭配等级、1d10000访客、经营快进，全部对标普利莫迪亚示例 - **标签词汇** — GRID_5/GRID_4、作物状态、陈酿状态、好感状态，全部取材自食味体验第二版 - **世界书路由** — § 锚点体系与普利莫迪亚 worldbook 完全对齐 - **调试面板** — 10项健康检查 + 存档查看/导出导入 + Action日志 + 局势摘要预览 ### 黄旗（2个低优先级观察） 1. **tick缺少 OFFLINE_LIMIT_MS** — 当前按钮驱动不影响，未来若改真实时间需加 2. **AI生成服务未联调** — `generateService.ts` 已定义但 `script.ts` 按钮未接入`requestAINarration()` ### 下一步 1. 完成 #7 设定补全（女主分布各城市 + 深界8+层域） 2. 桥接 AI 生成服务到 script.ts 按钮联调
- 下一步建议: 先完成 #7 设定补全，再联调 AI 生成服务
- 总体结论: 有条件通过

## 评审发现

### 核心架构五原则全部满足伪0层教程要求

- ID: F-核心架构五原则全部满足伪0层教程要求
- 严重级别: 低
- 分类: 其他
- 跟踪状态: 开放
- 相关里程碑: M1

### 酒馆接口适配层全部符合教程§4规范

- ID: F-酒馆接口适配层全部符合教程-4规范
- 严重级别: 低
- 分类: 其他
- 跟踪状态: 开放
- 相关里程碑: M2

### tick缺少离线结算上限

- ID: F-tick缺少离线结算上限
- 严重级别: 中
- 分类: 其他
- 跟踪状态: 开放
- 相关里程碑: M3
- 说明:

  教程§7建议：「必须有离线上限，不然玩家关掉酒馆三天再打开世界可能当场爆炸」。当前 `tavernTick.ts` 只在按钮点击时执行，无 `OFFLINE_LIMIT_MS` 保护。这属于未来扩展项（当前经营快进是按钮驱动，不是真实时间驱动，影响不大）。
- 建议:

  如未来改为真实时间驱动（如每次加载自动推进），需要加 OFFLINE_LIMIT_MS。
- 证据:
  - `src/tavern_status/engine/tavernTick.ts`

### AI生成服务已定义但script.ts未接入联调

- ID: F-ai生成服务已定义但script-ts未接入联调
- 严重级别: 低
- 分类: 其他
- 跟踪状态: 开放
- 相关里程碑: M4
- 说明:

  `generateService.ts` 的 `requestAINarration()` 和 `writeAIReplyToChat()` 已定义完整，但 `script.ts` 中的按钮只走 `setOutput()` 写文本到输出区，没有触发AI生成→显示回复的完整流程。教程要求的「引擎结算→局势摘要→AI叙事→聊天楼层」链条在后半段（AI叙事→聊天楼层）还未在script.ts中接通。这属于接口桥接层的事，不是规则层问题。
- 建议:

  后续在 script.ts 的按钮处理中接入 `requestAINarration()` + `writeAIReplyToChat()` 完成联调。
- 证据:
  - `src/tavern_status/services/generateService.ts`
  - `src/tavern_status/script.ts`

## 评审里程碑

### M1 · M1：伪0层核心架构对位（教程§1-3）

- 状态: 已完成
- 记录时间: 2026-05-19T02:13:22.380Z
- 摘要:

  核查教程第1-3节的伪0层核心五原则：

  ✅ **原则1「前端是客户端」** — `index.html` 有16个面板，`script.ts` 里的按钮都通过 `setOutput` 或 `dispatchAction` 走统一入口，玩家不直接接触变量。

  ✅ **原则2「chat变量是数据库」** — `saveService.ts` 的 `SAVE_KEY = 'primordia_save'` 存在 chat 变量中；`writeSave()` 用 `updateVariablesWith({ type: 'chat' })`；所有读写从同一入口走。

  ✅ **原则3「engine是规则裁判」** — `engine/` 下10个文件：cooking/sauce/drink/farming/brewing/customer/visitors/tavernTick/labels/healthCheck，不与AI上下文耦合。

  ✅ **原则4「dispatcher是唯一动作入口」** — `actions.ts` 定义了 25 个 GameAction 类型，`dispatcher.ts` 的 `dispatchAction(save, action)` 是唯一入口。

  ✅ **原则5「AI只做叙述者」** — `situationBuilder.ts` 产出纯文字局势摘要，`generateService.ts` 调用 generate，AI提示词里有 `[AI权限提示]` 约束。
- 结论:

  核查教程第1-3节的伪0层核心五原则： ✅ **原则1「前端是客户端」** — `index.html` 有16个面板，`script.ts` 里的按钮都通过 `setOutput` 或 `dispatchAction` 走统一入口，玩家不直接接触变量。 ✅ **原则2「chat变量是数据库」** — `saveService.ts` 的 `SAVE_KEY = 'primordia_save'` 存在 chat 变量中；`writeSave()` 用 `updateVariablesWith({ type: 'chat' })`；所有读写从同一入口走。 ✅ **原则3「engine是规则裁判」** — `engine/` 下10个文件：cooking/sauce/drink/farming/brewing/customer/visitors/tavernTick/labels/healthCheck，不与AI上下文耦合。 ✅ **原则4「dispatcher是唯一动作入口」** — `actions.ts` 定义了 25 个 GameAction 类型，`dispatcher.ts` 的 `dispatchAction(save, action)` 是唯一入口。 ✅ **原则5「AI只做叙述者」** — `situationBuilder.ts` 产出纯文字局势摘要，`generateService.ts` 调用 generate，AI提示词里有 `[AI权限提示]` 约束。
- 问题:
  - [低] 其他: 核心架构五原则全部满足伪0层教程要求

### M2 · M2：酒馆接口适配层核查（教程§4）

- 状态: 已完成
- 记录时间: 2026-05-19T02:15:46.180Z
- 摘要:

  核查教程第4节酒馆接口使用规范：

  ✅ **变量接口** — `saveService.ts` 用 `getVariables({ type: 'chat' })` + `updateVariablesWith()` 读写 chat 变量；所有读写从同一入口走，带 `SAVE_KEY` 常量。

  ✅ **聊天楼层接口** — `generateService.ts` 的 `writeAIReplyToChat()` 用 `createChatMessages()` 写入，role: assistant，不携带 data。

  ✅ **生成接口** — 使用 `generate()` 而非 `generateRaw`，带 `should_stream`、`injects` 注入权限边界。

  ✅ **注入接口** — `generateService.ts` 中 `injects` 数组含 `position: 'in_chat'` 的 `[AI权限提示]`；`worldbookRouter.ts` 构建扫描文本用于世界书激活（§锚点体系）。

  ✅ **事件接口** — `script.ts` 监听 `Mvu.events.VARIABLE_UPDATE_ENDED` 触发 `renderAllExtended()`。

  ✅ **世界书接口** — `worldbookRouter.ts` 输出 `§MAT_INDEX§ §AROMA_TAG§ §TASTE_EXP_V2§ §COOK_METHOD§ §PRICE_REF§` 锚点扫描文本，与示例普利莫迪亚的 § 锚点引用体系完全对齐。

  ⚠️ **一个观察** — 当前代码里 `vendorStockService.ts` 调用的是 AI generate 来生成摊贩货品，符合教程「AI可选权力：引入新顾客/商贩」，货品生成后走 `BUY_ITEMS` action 结算。
- 结论:

  核查教程第4节酒馆接口使用规范： ✅ **变量接口** — `saveService.ts` 用 `getVariables({ type: 'chat' })` + `updateVariablesWith()` 读写 chat 变量；所有读写从同一入口走，带 `SAVE_KEY` 常量。 ✅ **聊天楼层接口** — `generateService.ts` 的 `writeAIReplyToChat()` 用 `createChatMessages()` 写入，role: assistant，不携带 data。 ✅ **生成接口** — 使用 `generate()` 而非 `generateRaw`，带 `should_stream`、`injects` 注入权限边界。 ✅ **注入接口** — `generateService.ts` 中 `injects` 数组含 `position: 'in_chat'` 的 `[AI权限提示]`；`worldbookRouter.ts` 构建扫描文本用于世界书激活（§锚点体系）。 ✅ **事件接口** — `script.ts` 监听 `Mvu.events.VARIABLE_UPDATE_ENDED` 触发 `renderAllExtended()`。 ✅ **世界书接口** — `worldbookRouter.ts` 输出 `§MAT_INDEX§ §AROMA_TAG§ §TASTE_EXP_V2§ §COOK_METHOD§ §PRICE_REF§` 锚点扫描文本，与示例普利莫迪亚的 § 锚点引用体系完全对齐。 ⚠️ **一个观察** — 当前代码里 `vendorStockService.ts` 调用的是 AI generate 来生成摊贩货品，符合教程「AI可选权力：引入新顾客/商贩」，货品生成后走 `BUY_ITEMS` action 结算。
- 问题:
  - [低] 其他: 酒馆接口适配层全部符合教程§4规范

### M3 · M3：引擎层规则对标教程§6-7 + 普利莫迪亚世界观对齐

- 状态: 已完成
- 记录时间: 2026-05-19T02:16:32.225Z
- 摘要:

  核查教程第5-7节（目录结构/Action/tick）：

  ✅ **目录结构（教程§5）** — `engine/`（规则层）、`services/`（适配层）、`actions.ts`/`dispatcher.ts` 分离清晰。组件（script.ts+index.html）只做意图发出，不改存档。

  ✅ **Action骨架（教程§6）** — `actions.ts` 25种GameAction + `dispatcher.ts` 单一入口 switch-case，完全符合教程要求。

  ⚠️ **tick（教程§7）** — `tavernTick.ts` 有基础时间推进（客流判定→售出分配→收支结算），但缺少教程建议的离线结算上限（OFFLINE_LIMIT_MS）。

  世界观对齐核查（对照示例/普利莫迪亚）：

  ✅ **烹饪等级** — 代码8级（烧火工→灶火宗师），对齐灶火行会版引擎。✅

  ✅ **标签词汇** — GRID_5/GRID_4 的味觉格位词（「寡淡→咸苦发涩」等）完全来自食味体验第二版。✅

  ✅ **份量格位维度** — 5格维度（咸甜酸苦鲜辣辛凉）+ 4格维度（甘麻涩），与灶火行会版第2步完全一致。✅

  ✅ **搭配等级7级** — 灾难级→奇迹，与灶火行会版第5步一致。✅

  ✅ **访客引擎** — `visitors.ts` 的1d10000→ABCD 解码、B位等级表/C位诉求/D位氛围/团体关系，与普利莫迪亚「酒馆访客生成引擎（村庄版）」一致。✅

  ✅ **世界书路由** — `worldbookRouter.ts` 输出 §MAT_INDEX§ §AROMA_TAG§ 等锚点，与普利莫迪亚 worldbook 的 § 体系对齐。✅

  ✅ **经营快进** — `tavernTick.ts` 按声望等级分客流量区间，季节调整，8%打断概率，与普利莫迪亚经营快进引擎一致。✅
- 结论:

  核查教程第5-7节（目录结构/Action/tick）： ✅ **目录结构（教程§5）** — `engine/`（规则层）、`services/`（适配层）、`actions.ts`/`dispatcher.ts` 分离清晰。组件（script.ts+index.html）只做意图发出，不改存档。 ✅ **Action骨架（教程§6）** — `actions.ts` 25种GameAction + `dispatcher.ts` 单一入口 switch-case，完全符合教程要求。 ⚠️ **tick（教程§7）** — `tavernTick.ts` 有基础时间推进（客流判定→售出分配→收支结算），但缺少教程建议的离线结算上限（OFFLINE_LIMIT_MS）。 世界观对齐核查（对照示例/普利莫迪亚）： ✅ **烹饪等级** — 代码8级（烧火工→灶火宗师），对齐灶火行会版引擎。✅ ✅ **标签词汇** — GRID_5/GRID_4 的味觉格位词（「寡淡→咸苦发涩」等）完全来自食味体验第二版。✅ ✅ **份量格位维度** — 5格维度（咸甜酸苦鲜辣辛凉）+ 4格维度（甘麻涩），与灶火行会版第2步完全一致。✅ ✅ **搭配等级7级** — 灾难级→奇迹，与灶火行会版第5步一致。✅ ✅ **访客引擎** — `visitors.ts` 的1d10000→ABCD 解码、B位等级表/C位诉求/D位氛围/团体关系，与普利莫迪亚「酒馆访客生成引擎（村庄版）」一致。✅ ✅ **世界书路由** — `worldbookRouter.ts` 输出 §MAT_INDEX§ §AROMA_TAG§ 等锚点，与普利莫迪亚 worldbook 的 § 体系对齐。✅ ✅ **经营快进** — `tavernTick.ts` 按声望等级分客流量区间，季节调整，8%打断概率，与普利莫迪亚经营快进引擎一致。✅
- 问题:
  - [中] 其他: tick缺少离线结算上限

### M4 · M4：AI权限边界 + 调试面板 + 总结（教程§15-21）

- 状态: 已完成
- 记录时间: 2026-05-19T02:17:10.603Z
- 已审模块: engine/*, services/*, actions.ts, dispatcher.ts, script.ts, index.html, saveSchema.ts, labels.ts
- 摘要:

  核查教程§15-21（AI提示词/调试面板/制作顺序/总结）：

  ✅ **AI提示词（教程§15）** — `generateService.ts` 的 injects 中含：
  - 「你现在只负责AIRP、角色互动、叙述、建议和气氛」
  - 「以下内容由前端脚本决定，你不能更改、伪造或宣称已经完成」
  - 「最终回复只保留角色对玩家说的话或场景叙述」
  完全符合教程的AI权限边界硬约束。

  ✅ **调试面板（教程§17）** — 已实现（本期完成）：
  - 存档 JSON 只读查看 + 导出导入
  - 最近 Action 日志（环形缓冲20条）
  - 局势摘要预览
  - 健康检查（10项：金钱/库存/农田/酿酒/员工/关系/玩家/世界/酒馆/元数据）

  ✅ **制作顺序（教程§18）** — 当前执行顺序：
  1.✅ 玩法边界（S1设计文档）→ 2.✅ AI权限 → 3.✅ 主存档Schema → 4.✅ zod校验+迁移 →
  5.✅ 引擎规则 → 6.✅ dispatcher → 7.✅ 前端壳+界面 → 8.✅ generate+流式 →
  9.✅ 世界书路由 → 10.✅ 调试面板 → 11.待定 丰富AI人设和世界书
  与教程推荐的顺序高度一致。

  ✅ **大白话总结（教程§21）** — 代码完全满足总结中的8条原则：
  - 前端是客户端 ✅
  - chat变量是数据库 ✅
  - engine是规则裁判 ✅
  - dispatcher是唯一动作入口 ✅
  - 世界书由脚本路由 ✅
  - AI只做叙述者和角色 ✅
  - 聊天楼层是记录，不是主存档 ✅
  - MVU做楼层快照，不做大型玩法主权威 ✅

  ⚠️ **AI生成服务当前未接入** — `generateService.ts` 定义了 `requestAINarration()` 和 `writeAIReplyToChat()`，但 script.ts 中的按钮当前只走 `setOutput()` 输出文本到输出区，没有自动触发AI叙事。这意味着「引擎结算→AI叙事→显示」的完整链条还未联调。这属于已设计好但未连线的接口桥接。
- 结论:

  核查教程§15-21（AI提示词/调试面板/制作顺序/总结）： ✅ **AI提示词（教程§15）** — `generateService.ts` 的 injects 中含： - 「你现在只负责AIRP、角色互动、叙述、建议和气氛」 - 「以下内容由前端脚本决定，你不能更改、伪造或宣称已经完成」 - 「最终回复只保留角色对玩家说的话或场景叙述」 完全符合教程的AI权限边界硬约束。 ✅ **调试面板（教程§17）** — 已实现（本期完成）： - 存档 JSON 只读查看 + 导出导入 - 最近 Action 日志（环形缓冲20条） - 局势摘要预览 - 健康检查（10项：金钱/库存/农田/酿酒/员工/关系/玩家/世界/酒馆/元数据） ✅ **制作顺序（教程§18）** — 当前执行顺序： 1.✅ 玩法边界（S1设计文档）→ 2.✅ AI权限 → 3.✅ 主存档Schema → 4.✅ zod校验+迁移 → 5.✅ 引擎规则 → 6.✅ dispatcher → 7.✅ 前端壳+界面 → 8.✅ generate+流式 → 9.✅ 世界书路由 → 10.✅ 调试面板 → 11.待定 丰富AI人设和世界书 与教程推荐的顺序高度一致。 ✅ **大白话总结（教程§21）** — 代码完全满足总结中的8条原则： - 前端是客户端 ✅ - chat变量是数据库 ✅ - engine是规则裁判 ✅ - dispatcher是唯一动作入口 ✅ - 世界书由脚本路由 ✅ - AI只做叙述者和角色 ✅ - 聊天楼层是记录，不是主存档 ✅ - MVU做楼层快照，不做大型玩法主权威 ✅ ⚠️ **AI生成服务当前未接入** — `generateService.ts` 定义了 `requestAINarration()` 和 `writeAIReplyToChat()`，但 script.ts 中的按钮当前只走 `setOutput()` 输出文本到输出区，没有自动触发AI叙事。这意味着「引擎结算→AI叙事→显示」的完整链条还未联调。这属于已设计好但未连线的接口桥接。
- 下一步建议:

  完成剩余 TODO #7（设定补全：女主分布 + 深界8+层域），然后做 AI 生成连通联调。
- 问题:
  - [低] 其他: AI生成服务已定义但script.ts未接入联调

## 最终结论

## 总体结论

代码架构**完全符合**伪0层教程21条原则，普利莫迪亚世界观对齐度**极高**。

### 绿旗（全通过）
- **核心架构5原则** — 前端客户端 → chat变量数据库 → engine裁判 → dispatcher唯一入口 → AI纯叙述者
- **酒馆接口7维** — 变量/楼层/生成/注入/事件/世界书全部正确使用
- **引擎规则** — 8级灶火行会烹饪、7级搭配等级、1d10000访客、经营快进，全部对标普利莫迪亚示例
- **标签词汇** — GRID_5/GRID_4、作物状态、陈酿状态、好感状态，全部取材自食味体验第二版
- **世界书路由** — § 锚点体系与普利莫迪亚 worldbook 完全对齐
- **调试面板** — 10项健康检查 + 存档查看/导出导入 + Action日志 + 局势摘要预览

### 黄旗（2个低优先级观察）
1. **tick缺少 OFFLINE_LIMIT_MS** — 当前按钮驱动不影响，未来若改真实时间需加
2. **AI生成服务未联调** — `generateService.ts` 已定义但 `script.ts` 按钮未接入`requestAINarration()`

### 下一步
1. 完成 #7 设定补全（女主分布各城市 + 深界8+层域）
2. 桥接 AI 生成服务到 script.ts 按钮联调

## 评审快照

```json
{
  "formatVersion": 4,
  "kind": "limcode.review",
  "reviewRunId": "review-mpc009b9-ak9vzz",
  "createdAt": "2026-05-19T00:00:00.000Z",
  "updatedAt": "2026-05-19T02:17:30.752Z",
  "finalizedAt": "2026-05-19T02:17:30.752Z",
  "status": "completed",
  "overallDecision": "conditionally_accepted",
  "header": {
    "title": "普利莫迪亚沙盒卡 — 架构合规性与世界观对齐评审",
    "date": "2026-05-19",
    "overview": "对照教程21条伪0层原则和示例/普利莫迪亚世界观，逐模块审查代码架构"
  },
  "scope": {
    "markdown": "# 普利莫迪亚沙盒卡 — 架构合规性与世界观对齐评审\n\n> 评审日期：2026-05-18\n> 对照基准：大型前端卡AIRP伪0层玩法教程 + 示例/普利莫迪亚\n\n---\n\n## 评审范围\n\n- `engine/` — 规则裁判层\n- `services/` — 酒馆接口适配层\n- `dispatcher.ts` / `actions.ts` — 统一动作派发\n- `script.ts` / `index.html` — 前端客户端\n- `saveSchema.ts` — 主存档结构"
  },
  "summary": {
    "latestConclusion": "## 总体结论\n\n代码架构**完全符合**伪0层教程21条原则，普利莫迪亚世界观对齐度**极高**。\n\n### 绿旗（全通过）\n- **核心架构5原则** — 前端客户端 → chat变量数据库 → engine裁判 → dispatcher唯一入口 → AI纯叙述者\n- **酒馆接口7维** — 变量/楼层/生成/注入/事件/世界书全部正确使用\n- **引擎规则** — 8级灶火行会烹饪、7级搭配等级、1d10000访客、经营快进，全部对标普利莫迪亚示例\n- **标签词汇** — GRID_5/GRID_4、作物状态、陈酿状态、好感状态，全部取材自食味体验第二版\n- **世界书路由** — § 锚点体系与普利莫迪亚 worldbook 完全对齐\n- **调试面板** — 10项健康检查 + 存档查看/导出导入 + Action日志 + 局势摘要预览\n\n### 黄旗（2个低优先级观察）\n1. **tick缺少 OFFLINE_LIMIT_MS** — 当前按钮驱动不影响，未来若改真实时间需加\n2. **AI生成服务未联调** — `generateService.ts` 已定义但 `script.ts` 按钮未接入`requestAINarration()`\n\n### 下一步\n1. 完成 #7 设定补全（女主分布各城市 + 深界8+层域）\n2. 桥接 AI 生成服务到 script.ts 按钮联调",
    "recommendedNextAction": "先完成 #7 设定补全，再联调 AI 生成服务",
    "reviewedModules": [
      "engine/*",
      "services/*",
      "actions.ts",
      "dispatcher.ts",
      "script.ts",
      "index.html",
      "saveSchema.ts",
      "labels.ts"
    ]
  },
  "stats": {
    "totalMilestones": 4,
    "completedMilestones": 4,
    "totalFindings": 4,
    "severity": {
      "high": 0,
      "medium": 1,
      "low": 3
    }
  },
  "milestones": [
    {
      "id": "M1",
      "title": "M1：伪0层核心架构对位（教程§1-3）",
      "status": "completed",
      "recordedAt": "2026-05-19T02:13:22.380Z",
      "summaryMarkdown": "核查教程第1-3节的伪0层核心五原则：\n\n✅ **原则1「前端是客户端」** — `index.html` 有16个面板，`script.ts` 里的按钮都通过 `setOutput` 或 `dispatchAction` 走统一入口，玩家不直接接触变量。\n\n✅ **原则2「chat变量是数据库」** — `saveService.ts` 的 `SAVE_KEY = 'primordia_save'` 存在 chat 变量中；`writeSave()` 用 `updateVariablesWith({ type: 'chat' })`；所有读写从同一入口走。\n\n✅ **原则3「engine是规则裁判」** — `engine/` 下10个文件：cooking/sauce/drink/farming/brewing/customer/visitors/tavernTick/labels/healthCheck，不与AI上下文耦合。\n\n✅ **原则4「dispatcher是唯一动作入口」** — `actions.ts` 定义了 25 个 GameAction 类型，`dispatcher.ts` 的 `dispatchAction(save, action)` 是唯一入口。\n\n✅ **原则5「AI只做叙述者」** — `situationBuilder.ts` 产出纯文字局势摘要，`generateService.ts` 调用 generate，AI提示词里有 `[AI权限提示]` 约束。",
      "conclusionMarkdown": "核查教程第1-3节的伪0层核心五原则： ✅ **原则1「前端是客户端」** — `index.html` 有16个面板，`script.ts` 里的按钮都通过 `setOutput` 或 `dispatchAction` 走统一入口，玩家不直接接触变量。 ✅ **原则2「chat变量是数据库」** — `saveService.ts` 的 `SAVE_KEY = 'primordia_save'` 存在 chat 变量中；`writeSave()` 用 `updateVariablesWith({ type: 'chat' })`；所有读写从同一入口走。 ✅ **原则3「engine是规则裁判」** — `engine/` 下10个文件：cooking/sauce/drink/farming/brewing/customer/visitors/tavernTick/labels/healthCheck，不与AI上下文耦合。 ✅ **原则4「dispatcher是唯一动作入口」** — `actions.ts` 定义了 25 个 GameAction 类型，`dispatcher.ts` 的 `dispatchAction(save, action)` 是唯一入口。 ✅ **原则5「AI只做叙述者」** — `situationBuilder.ts` 产出纯文字局势摘要，`generateService.ts` 调用 generate，AI提示词里有 `[AI权限提示]` 约束。",
      "evidence": [],
      "reviewedModules": [],
      "recommendedNextAction": null,
      "findingIds": [
        "F-核心架构五原则全部满足伪0层教程要求"
      ]
    },
    {
      "id": "M2",
      "title": "M2：酒馆接口适配层核查（教程§4）",
      "status": "completed",
      "recordedAt": "2026-05-19T02:15:46.180Z",
      "summaryMarkdown": "核查教程第4节酒馆接口使用规范：\n\n✅ **变量接口** — `saveService.ts` 用 `getVariables({ type: 'chat' })` + `updateVariablesWith()` 读写 chat 变量；所有读写从同一入口走，带 `SAVE_KEY` 常量。\n\n✅ **聊天楼层接口** — `generateService.ts` 的 `writeAIReplyToChat()` 用 `createChatMessages()` 写入，role: assistant，不携带 data。\n\n✅ **生成接口** — 使用 `generate()` 而非 `generateRaw`，带 `should_stream`、`injects` 注入权限边界。\n\n✅ **注入接口** — `generateService.ts` 中 `injects` 数组含 `position: 'in_chat'` 的 `[AI权限提示]`；`worldbookRouter.ts` 构建扫描文本用于世界书激活（§锚点体系）。\n\n✅ **事件接口** — `script.ts` 监听 `Mvu.events.VARIABLE_UPDATE_ENDED` 触发 `renderAllExtended()`。\n\n✅ **世界书接口** — `worldbookRouter.ts` 输出 `§MAT_INDEX§ §AROMA_TAG§ §TASTE_EXP_V2§ §COOK_METHOD§ §PRICE_REF§` 锚点扫描文本，与示例普利莫迪亚的 § 锚点引用体系完全对齐。\n\n⚠️ **一个观察** — 当前代码里 `vendorStockService.ts` 调用的是 AI generate 来生成摊贩货品，符合教程「AI可选权力：引入新顾客/商贩」，货品生成后走 `BUY_ITEMS` action 结算。",
      "conclusionMarkdown": "核查教程第4节酒馆接口使用规范： ✅ **变量接口** — `saveService.ts` 用 `getVariables({ type: 'chat' })` + `updateVariablesWith()` 读写 chat 变量；所有读写从同一入口走，带 `SAVE_KEY` 常量。 ✅ **聊天楼层接口** — `generateService.ts` 的 `writeAIReplyToChat()` 用 `createChatMessages()` 写入，role: assistant，不携带 data。 ✅ **生成接口** — 使用 `generate()` 而非 `generateRaw`，带 `should_stream`、`injects` 注入权限边界。 ✅ **注入接口** — `generateService.ts` 中 `injects` 数组含 `position: 'in_chat'` 的 `[AI权限提示]`；`worldbookRouter.ts` 构建扫描文本用于世界书激活（§锚点体系）。 ✅ **事件接口** — `script.ts` 监听 `Mvu.events.VARIABLE_UPDATE_ENDED` 触发 `renderAllExtended()`。 ✅ **世界书接口** — `worldbookRouter.ts` 输出 `§MAT_INDEX§ §AROMA_TAG§ §TASTE_EXP_V2§ §COOK_METHOD§ §PRICE_REF§` 锚点扫描文本，与示例普利莫迪亚的 § 锚点引用体系完全对齐。 ⚠️ **一个观察** — 当前代码里 `vendorStockService.ts` 调用的是 AI generate 来生成摊贩货品，符合教程「AI可选权力：引入新顾客/商贩」，货品生成后走 `BUY_ITEMS` action 结算。",
      "evidence": [],
      "reviewedModules": [],
      "recommendedNextAction": null,
      "findingIds": [
        "F-酒馆接口适配层全部符合教程-4规范"
      ]
    },
    {
      "id": "M3",
      "title": "M3：引擎层规则对标教程§6-7 + 普利莫迪亚世界观对齐",
      "status": "completed",
      "recordedAt": "2026-05-19T02:16:32.225Z",
      "summaryMarkdown": "核查教程第5-7节（目录结构/Action/tick）：\n\n✅ **目录结构（教程§5）** — `engine/`（规则层）、`services/`（适配层）、`actions.ts`/`dispatcher.ts` 分离清晰。组件（script.ts+index.html）只做意图发出，不改存档。\n\n✅ **Action骨架（教程§6）** — `actions.ts` 25种GameAction + `dispatcher.ts` 单一入口 switch-case，完全符合教程要求。\n\n⚠️ **tick（教程§7）** — `tavernTick.ts` 有基础时间推进（客流判定→售出分配→收支结算），但缺少教程建议的离线结算上限（OFFLINE_LIMIT_MS）。\n\n世界观对齐核查（对照示例/普利莫迪亚）：\n\n✅ **烹饪等级** — 代码8级（烧火工→灶火宗师），对齐灶火行会版引擎。✅\n\n✅ **标签词汇** — GRID_5/GRID_4 的味觉格位词（「寡淡→咸苦发涩」等）完全来自食味体验第二版。✅\n\n✅ **份量格位维度** — 5格维度（咸甜酸苦鲜辣辛凉）+ 4格维度（甘麻涩），与灶火行会版第2步完全一致。✅\n\n✅ **搭配等级7级** — 灾难级→奇迹，与灶火行会版第5步一致。✅\n\n✅ **访客引擎** — `visitors.ts` 的1d10000→ABCD 解码、B位等级表/C位诉求/D位氛围/团体关系，与普利莫迪亚「酒馆访客生成引擎（村庄版）」一致。✅\n\n✅ **世界书路由** — `worldbookRouter.ts` 输出 §MAT_INDEX§ §AROMA_TAG§ 等锚点，与普利莫迪亚 worldbook 的 § 体系对齐。✅\n\n✅ **经营快进** — `tavernTick.ts` 按声望等级分客流量区间，季节调整，8%打断概率，与普利莫迪亚经营快进引擎一致。✅",
      "conclusionMarkdown": "核查教程第5-7节（目录结构/Action/tick）： ✅ **目录结构（教程§5）** — `engine/`（规则层）、`services/`（适配层）、`actions.ts`/`dispatcher.ts` 分离清晰。组件（script.ts+index.html）只做意图发出，不改存档。 ✅ **Action骨架（教程§6）** — `actions.ts` 25种GameAction + `dispatcher.ts` 单一入口 switch-case，完全符合教程要求。 ⚠️ **tick（教程§7）** — `tavernTick.ts` 有基础时间推进（客流判定→售出分配→收支结算），但缺少教程建议的离线结算上限（OFFLINE_LIMIT_MS）。 世界观对齐核查（对照示例/普利莫迪亚）： ✅ **烹饪等级** — 代码8级（烧火工→灶火宗师），对齐灶火行会版引擎。✅ ✅ **标签词汇** — GRID_5/GRID_4 的味觉格位词（「寡淡→咸苦发涩」等）完全来自食味体验第二版。✅ ✅ **份量格位维度** — 5格维度（咸甜酸苦鲜辣辛凉）+ 4格维度（甘麻涩），与灶火行会版第2步完全一致。✅ ✅ **搭配等级7级** — 灾难级→奇迹，与灶火行会版第5步一致。✅ ✅ **访客引擎** — `visitors.ts` 的1d10000→ABCD 解码、B位等级表/C位诉求/D位氛围/团体关系，与普利莫迪亚「酒馆访客生成引擎（村庄版）」一致。✅ ✅ **世界书路由** — `worldbookRouter.ts` 输出 §MAT_INDEX§ §AROMA_TAG§ 等锚点，与普利莫迪亚 worldbook 的 § 体系对齐。✅ ✅ **经营快进** — `tavernTick.ts` 按声望等级分客流量区间，季节调整，8%打断概率，与普利莫迪亚经营快进引擎一致。✅",
      "evidence": [],
      "reviewedModules": [],
      "recommendedNextAction": null,
      "findingIds": [
        "F-tick缺少离线结算上限"
      ]
    },
    {
      "id": "M4",
      "title": "M4：AI权限边界 + 调试面板 + 总结（教程§15-21）",
      "status": "completed",
      "recordedAt": "2026-05-19T02:17:10.603Z",
      "summaryMarkdown": "核查教程§15-21（AI提示词/调试面板/制作顺序/总结）：\n\n✅ **AI提示词（教程§15）** — `generateService.ts` 的 injects 中含：\n- 「你现在只负责AIRP、角色互动、叙述、建议和气氛」\n- 「以下内容由前端脚本决定，你不能更改、伪造或宣称已经完成」\n- 「最终回复只保留角色对玩家说的话或场景叙述」\n完全符合教程的AI权限边界硬约束。\n\n✅ **调试面板（教程§17）** — 已实现（本期完成）：\n- 存档 JSON 只读查看 + 导出导入\n- 最近 Action 日志（环形缓冲20条）\n- 局势摘要预览\n- 健康检查（10项：金钱/库存/农田/酿酒/员工/关系/玩家/世界/酒馆/元数据）\n\n✅ **制作顺序（教程§18）** — 当前执行顺序：\n1.✅ 玩法边界（S1设计文档）→ 2.✅ AI权限 → 3.✅ 主存档Schema → 4.✅ zod校验+迁移 →\n5.✅ 引擎规则 → 6.✅ dispatcher → 7.✅ 前端壳+界面 → 8.✅ generate+流式 →\n9.✅ 世界书路由 → 10.✅ 调试面板 → 11.待定 丰富AI人设和世界书\n与教程推荐的顺序高度一致。\n\n✅ **大白话总结（教程§21）** — 代码完全满足总结中的8条原则：\n- 前端是客户端 ✅\n- chat变量是数据库 ✅\n- engine是规则裁判 ✅\n- dispatcher是唯一动作入口 ✅\n- 世界书由脚本路由 ✅\n- AI只做叙述者和角色 ✅\n- 聊天楼层是记录，不是主存档 ✅\n- MVU做楼层快照，不做大型玩法主权威 ✅\n\n⚠️ **AI生成服务当前未接入** — `generateService.ts` 定义了 `requestAINarration()` 和 `writeAIReplyToChat()`，但 script.ts 中的按钮当前只走 `setOutput()` 输出文本到输出区，没有自动触发AI叙事。这意味着「引擎结算→AI叙事→显示」的完整链条还未联调。这属于已设计好但未连线的接口桥接。",
      "conclusionMarkdown": "核查教程§15-21（AI提示词/调试面板/制作顺序/总结）： ✅ **AI提示词（教程§15）** — `generateService.ts` 的 injects 中含： - 「你现在只负责AIRP、角色互动、叙述、建议和气氛」 - 「以下内容由前端脚本决定，你不能更改、伪造或宣称已经完成」 - 「最终回复只保留角色对玩家说的话或场景叙述」 完全符合教程的AI权限边界硬约束。 ✅ **调试面板（教程§17）** — 已实现（本期完成）： - 存档 JSON 只读查看 + 导出导入 - 最近 Action 日志（环形缓冲20条） - 局势摘要预览 - 健康检查（10项：金钱/库存/农田/酿酒/员工/关系/玩家/世界/酒馆/元数据） ✅ **制作顺序（教程§18）** — 当前执行顺序： 1.✅ 玩法边界（S1设计文档）→ 2.✅ AI权限 → 3.✅ 主存档Schema → 4.✅ zod校验+迁移 → 5.✅ 引擎规则 → 6.✅ dispatcher → 7.✅ 前端壳+界面 → 8.✅ generate+流式 → 9.✅ 世界书路由 → 10.✅ 调试面板 → 11.待定 丰富AI人设和世界书 与教程推荐的顺序高度一致。 ✅ **大白话总结（教程§21）** — 代码完全满足总结中的8条原则： - 前端是客户端 ✅ - chat变量是数据库 ✅ - engine是规则裁判 ✅ - dispatcher是唯一动作入口 ✅ - 世界书由脚本路由 ✅ - AI只做叙述者和角色 ✅ - 聊天楼层是记录，不是主存档 ✅ - MVU做楼层快照，不做大型玩法主权威 ✅ ⚠️ **AI生成服务当前未接入** — `generateService.ts` 定义了 `requestAINarration()` 和 `writeAIReplyToChat()`，但 script.ts 中的按钮当前只走 `setOutput()` 输出文本到输出区，没有自动触发AI叙事。这意味着「引擎结算→AI叙事→显示」的完整链条还未联调。这属于已设计好但未连线的接口桥接。",
      "evidence": [],
      "reviewedModules": [
        "engine/*",
        "services/*",
        "actions.ts",
        "dispatcher.ts",
        "script.ts",
        "index.html",
        "saveSchema.ts",
        "labels.ts"
      ],
      "recommendedNextAction": "完成剩余 TODO #7（设定补全：女主分布 + 深界8+层域），然后做 AI 生成连通联调。",
      "findingIds": [
        "F-ai生成服务已定义但script-ts未接入联调"
      ]
    }
  ],
  "findings": [
    {
      "id": "F-核心架构五原则全部满足伪0层教程要求",
      "severity": "low",
      "category": "other",
      "title": "核心架构五原则全部满足伪0层教程要求",
      "descriptionMarkdown": null,
      "recommendationMarkdown": null,
      "evidence": [],
      "relatedMilestoneIds": [
        "M1"
      ],
      "trackingStatus": "open"
    },
    {
      "id": "F-酒馆接口适配层全部符合教程-4规范",
      "severity": "low",
      "category": "other",
      "title": "酒馆接口适配层全部符合教程§4规范",
      "descriptionMarkdown": null,
      "recommendationMarkdown": null,
      "evidence": [],
      "relatedMilestoneIds": [
        "M2"
      ],
      "trackingStatus": "open"
    },
    {
      "id": "F-tick缺少离线结算上限",
      "severity": "medium",
      "category": "other",
      "title": "tick缺少离线结算上限",
      "descriptionMarkdown": "教程§7建议：「必须有离线上限，不然玩家关掉酒馆三天再打开世界可能当场爆炸」。当前 `tavernTick.ts` 只在按钮点击时执行，无 `OFFLINE_LIMIT_MS` 保护。这属于未来扩展项（当前经营快进是按钮驱动，不是真实时间驱动，影响不大）。",
      "recommendationMarkdown": "如未来改为真实时间驱动（如每次加载自动推进），需要加 OFFLINE_LIMIT_MS。",
      "evidence": [
        {
          "path": "src/tavern_status/engine/tavernTick.ts"
        }
      ],
      "relatedMilestoneIds": [
        "M3"
      ],
      "trackingStatus": "open"
    },
    {
      "id": "F-ai生成服务已定义但script-ts未接入联调",
      "severity": "low",
      "category": "other",
      "title": "AI生成服务已定义但script.ts未接入联调",
      "descriptionMarkdown": "`generateService.ts` 的 `requestAINarration()` 和 `writeAIReplyToChat()` 已定义完整，但 `script.ts` 中的按钮只走 `setOutput()` 写文本到输出区，没有触发AI生成→显示回复的完整流程。教程要求的「引擎结算→局势摘要→AI叙事→聊天楼层」链条在后半段（AI叙事→聊天楼层）还未在script.ts中接通。这属于接口桥接层的事，不是规则层问题。",
      "recommendationMarkdown": "后续在 script.ts 的按钮处理中接入 `requestAINarration()` + `writeAIReplyToChat()` 完成联调。",
      "evidence": [
        {
          "path": "src/tavern_status/services/generateService.ts"
        },
        {
          "path": "src/tavern_status/script.ts"
        }
      ],
      "relatedMilestoneIds": [
        "M4"
      ],
      "trackingStatus": "open"
    }
  ],
  "render": {
    "rendererVersion": 4,
    "bodyHash": "sha256:570d56629dcef6dd5a136365faf4de6afb938a9a07d6b57ecb9cf2e033fe2427",
    "generatedAt": "2026-05-19T02:17:30.752Z",
    "locale": "zh-CN"
  }
}
```
